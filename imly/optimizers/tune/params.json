{
    "params": {
        "LogisticRegression": {
            "config": {
                "units": 1,
                "batch_size": 10,
                "epochs": 10,
                "optimizer": "adam",
                "losses": "binary_crossentropy",
                "activation": "sigmoid"
            },
            "val_metric": "val_loss",
            "metric": "accuracy"
        }
    }
}